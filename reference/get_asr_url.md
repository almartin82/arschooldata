# Get Annual Statistical Report URL for a given year

Returns the direct download URL for the ASR Excel file. URLs are not
consistent across years, so this function maintains the mapping.

## Usage

``` r
get_asr_url(end_year)
```

## Arguments

- end_year:

  School year end

## Value

URL string or NULL if not available
