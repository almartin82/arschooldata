# Standardize Arkansas LEA/School IDs

Arkansas uses 7-digit IDs for districts (LEAs) and schools. This
function ensures IDs are properly formatted as 7-digit strings.

## Usage

``` r
standardize_ar_id(id)
```

## Arguments

- id:

  Character or numeric ID

## Value

Character ID padded to 7 digits
